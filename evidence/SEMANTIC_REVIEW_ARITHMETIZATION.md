# Arithmetization — opening batch, 2026-09-04

## OLP-0041–0044 driver, integers, rationals and real-line motivation

Reread the complete frozen source for all four files and visually reread NSOU EMT-03 PDF323 (printed 318), PDF367–368 (printed 362–363), and the recovered West Bengal algebra page PDF189 before drafting. BN-IN-P029 records the direct integer-system terminology from PDF323. The equivalence-class and algebra pages support the construction and proof prose. They do not attest the specialized chapter-title sense, ordered-field terminology or completeness vocabulary; those choices remain explicitly provisional in T048–T051.

All 31 source/target blocks pass formula, protected-control, environment, semantic-token and NFC checks. OLP-0043 has one exact documented formula correction, BN-SRC-006; every other source formula remains byte-equivalent after whitespace normalization. Commented source material in OLP-0042 and OLP-0044 remains commented and is translated without being activated.

Reverse paraphrase for integers: represent the intended difference a-b by a pair (a,b), and identify two pairs exactly when their cross-sums agree. This relation is reflexive, symmetric and transitive. Integers are its equivalence classes. Addition, multiplication and order are defined on representatives, with later work responsible for proving representative independence. The map n to the class of (n,0) preserves addition, multiplication and order, so it lets the construction treat naturals as integers without asserting that the underlying sets are literally identical.

Reverse paraphrase for rationals: use integer pairs with nonzero second coordinate and identify (a,b) with (c,d) exactly when ad=bc. Rational addition and multiplication use the usual common-denominator formulas. The order condition says r is no greater than s when s-r has a representative with nonnegative numerator and positive denominator. BN-SRC-006 repairs the frozen prose's isolated reversal to r-s; the displayed class equation already uses s-r. The embedding sends i to the class of (i,1).

Reverse paraphrase for reals: rationals and reals are both ordered fields, but the reals include irrational values and satisfy the least-upper-bound completeness property. The two proofs that square root of two is irrational are an infinite-descent geometry argument and the standard parity contradiction. The displayed rational lower set has rational upper bounds but no least rational upper bound, motivating completion of the rational line.

BN-SRC-007 removes a stray less-than character after the element token; no inequality is altered. BN-SRC-008 makes the geometric descent's initial minimal-pair choice explicit, matching the hypothesis invoked at its conclusion. The frozen source's disabled footnote says the symbol for square root of two refers equally to both roots; standard notation instead denotes the nonnegative principal root. The footnote remains disabled in the aligned translation and is not adopted as reader mathematics. The source's disabled algebra sketch also switches from r to q; its formulas remain inactive and unchanged. These two inactive-source observations do not create a review hold.

No PDF, semantic HTML or release claim is made for this batch. The built reader still stops after Functions, and the last verified artifact release remains the 18-unit Sets and Relations edition.

## OLP-0045–0048 cuts, reflections, verification and Cauchy construction

Reread the complete frozen source for all four files and applied the same university pages already recorded for the chapter: direct integer, rational and real terminology; set, relation, ordered-pair and mapping definitions; equivalence-class proof language; algebraic operations and order; sequences and least-upper-bound prose. These pages support ordinary Indian Bengali mathematical diction. They do not directly attest every specialized compound. Dedekind cut, ordered ring, ordered field, Cauchy sequence and the philosophical title remain explicit provisional decisions T052–T055.

All 67 source/target blocks pass formula, protected-control, environment, semantic-token and NFC checks. OLP-0048 has one exact documented formula correction, BN-SRC-014. Every other active source formula remains byte-equivalent after whitespace normalization. Source comments in OLP-0047 stay comments; the translation does not silently activate the proposed subtraction or division definitions.

Reverse paraphrase for cuts: a real number is represented by a nonempty proper downward-closed subset of the rationals with no greatest member. Rational numbers embed as their strict lower sets. The cut operations and order are defined directly, and the least-upper-bound theorem takes the union of a nonempty bounded family of cuts. BN-SRC-009 replaces the source's incorrect appeal to boundedness when proving this union nonempty with the actual nonemptiness hypothesis. BN-SRC-010 preserves the displayed superscript-zero notation while identifying its type-correct intended object as the embedded real zero cut.

Reverse paraphrase for reflections: the constructions place naturals inside integers, integers inside rationals and rationals inside reals through structure-preserving embeddings. This justifies ordinary identification in practice while distinguishing the constructed representatives from literal set-theoretic identity. The philosophical questions and examples remain questions rather than asserted technical conclusions.

Reverse paraphrase for checking details: the exercises ask the reader to verify that the quotient operations do not depend on representatives and that the integer, rational and real constructions satisfy the relevant ordered algebraic laws. BN-SRC-011 states the conventional field requirement that zero and one differ, which excludes the one-element zero ring. BN-SRC-012 records that the active exercise mentions subtraction and division although the only proposed cut formulas for them are commented out; those operations must be supplied before that exercise can be completed.

Reverse paraphrase for Cauchy sequences: rational Cauchy sequences are identified when their difference tends to zero, and the real-number objects are the resulting equivalence classes. Addition, multiplication, order and rational embedding are defined on these classes. Completeness is sketched by recursively trapping values between rational endpoints whose gap tends to zero. BN-SRC-013 corrects relation to equivalence class as the represented object. BN-SRC-014 changes the positivity comparison from rational zero to real zero under the exact validator exception. BN-SRC-015 names equivalence classes, rather than raw sequences, as the ordered-field elements. The proof later uses representative sequences as shorthand inside set and bracket notation; the surrounding quotient definitions determine the intended types. BN-SRC-016 weakens the source's claim that the endpoint gap is exactly halved at every step to the valid bound that it is at most halved: if a midpoint already occurs among the values, both endpoints can move to it and the new gap is zero. This estimate still proves convergence.

No PDF, semantic HTML or reader-release claim is made for Arithmetization. The current public source checkpoint still contains 39 files until this eight-file batch and its evidence receive an immutable public readback. The last verified artifact release remains the 18-unit Sets and Relations edition.
