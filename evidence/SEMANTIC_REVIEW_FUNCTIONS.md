# Functions review — first continuation, 2026-09-04

OLP-0020 driver and OLP-0021 Basics drafted after personally consulting NSOU PDF370 and recovered WB PDF189/227 and Tripura PDF78. School prose supports variable/expression explanations; university definition governs domain, codomain and range vocabulary. The source mathematics remains authoritative.

Reverse paraphrase: each domain element has exactly one output in the codomain. Range contains the actually obtained outputs, so it may be smaller than codomain. Multiplication receives pairs and covers all naturals via n times one. Unrestricted square-root relation has two outputs for positive n. Students' grades versus parents distinguish unique output from zero/multiple outputs. Equal function values give function equality only when domain and codomain agree. Cases must cover every input exactly once. All exercises, figure caption, formal tokens, repeated variables and source conditional text retained.

Source issues retained explicitly for the next cumulative reader notes: the square-root example says positive square root although the zero-inclusive natural domain requires the nonnegative root at zero. The g example starts with n then changes to x without explanation. Both are present in the pristine English; do not silently strengthen or alter formulas. Add reader notes before publishing this chapter. No Functions reader build or publication is claimed yet.
## Remaining Functions sections, 2026-09-04

OLP-0022–0026 were compared against their complete pristine English files after visually consulting NSOU PDF371–374. The source, paragraph, formula, protected-control, environment, token and NFC checks pass for all 25 translated units. Math checking now explicitly retains nested math inside translated text captions; semantic comparison covers the surrounding quantifiers and conditions.

Reverse paraphrase samples: surjectivity means at least one preimage for every codomain value; injectivity means at most one. Their conjunction pairs each codomain value with exactly one domain element. The halving/rounding function is onto the zero-inclusive naturals and identifies 1 and 2; successor misses zero. A graph is a function graph precisely when each input has exactly one output. Restricting a function to C keeps outputs in B while restricting inputs to C. The two inverse equations have different directions; a right-inverse construction for an arbitrary surjection uses Choice, with the original natural-number, finite and bijective exceptions retained. A bijection's inverse is unique. Composition applies f before g, and its graph is R_f | R_g. Partial functions allow zero or one output, with actual domain contained in the ambient A; seriality supplies existence, not uniqueness.

Source issues for explicit reader notes: the left-inverse proposition is false for A empty and B nonempty; its proof chooses a member of A. Nonempty A repairs the statement, while A=B=empty has the unique empty inverse. The previous relations chapter restricts both coordinates to C, whereas function restriction here restricts only the first coordinate. The source's casual claim of exact agreement must not conceal that difference. Earlier source issues concerning positive square root at zero and the n/x slip remain recorded above. Translation preserves source formulas and labels; notes are separated in reader rendering.

Function extensionality versus a bare graph: the source requires equal domain and codomain to infer function equality. Its informal identification with graphs must retain the declared typing, because equal pair sets alone do not recover a chosen codomain. This is explained in the reader note rather than silently revising the source.

Conditional branches in OLP-0023/0025 use complete Bengali clauses rather than the source's English w/W split. Both outcomes preserve coherent sentences. Every exercise and the entire nested conditional Choice footnote are retained. No independent human review or new publication is claimed by this draft review.
