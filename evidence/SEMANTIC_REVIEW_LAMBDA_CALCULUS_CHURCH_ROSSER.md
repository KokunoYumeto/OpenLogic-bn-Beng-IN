# Semantic review: complete Lambda Calculus Church--Rosser chapter

Review date: 2026-09-20

Locale: `bn-Beng-IN`

Units: OLP-0367--OLP-0372

Scope: 72 aligned source/target blocks across the complete Church--Rosser chapter: the chapter driver, definitions and properties, parallel beta reduction, ordinary beta reduction, parallel beta-eta reduction, and ordinary beta-eta reduction. Of these, 65 blocks contain reviewed Bengali translation and seven are unchanged structural or formal blocks.

The Bengali text was read against the frozen English blocks in source order and reverse-paraphrased at the level of the Church--Rosser joining property, the computational interpretation of normal forms, uniqueness of normal form, the transitive-closure grid proof, the inductive parallel-reduction rules, simultaneous contraction and later-created redexes, reflexivity, beta and beta-eta complete development, compatibility with substitution, the continuation lemmas, the correspondence between parallel and ordinary reduction, and the two resulting Church--Rosser theorems. Every source/target pair passes block-count, audited-mathematics, protected-control, environment, semantic-token, and Unicode NFC checks. This is a source and semantic review by the primary translation model; no independent human review, TeX-engine result, or PDF result is claimed.

## Frozen-source identity

The controlling source is OpenLogic revision `9620cc73f9c8e0ad003c514a5d3748f29611c4c0`. Its 722-unit manifest has SHA-256 `5a6fef5c16c15a5b2f90f874c268512cfd6ed2e846bdfa850a67304a4c05a155`. Index generation revalidates the per-file source hashes before accepting these units.

## Unit coverage

| Unit | File or role | Blocks | Translated | Review focus |
|---|---|---:|---:|---|
| OLP-0367 | Church--Rosser chapter driver | 3 | 1 | Bengali chapter identity and all five imports in frozen-source order |
| OLP-0368 | Definitions and properties | 12 | 11 | Joining property, computation interpretation, unique normal forms, algebra example, and transitive-closure grid |
| OLP-0369 | Parallel beta reduction | 18 | 17 | Four inductive rules, later-created redexes, reflexivity, complete development, substitution compatibility, continuation, and Church--Rosser |
| OLP-0370 | Ordinary beta reduction | 11 | 10 | Inclusion in parallel reduction, simulation of all four parallel rules, equality of transitive closures, and Church--Rosser transfer |
| OLP-0371 | Parallel beta-eta reduction | 16 | 15 | Five inductive rules, complete-development clause priority, substitution compatibility, continuation, and Church--Rosser |
| OLP-0372 | Ordinary beta-eta reduction | 12 | 11 | Mixed one-step beta/eta contraction, parallel simulation, equality of transitive closures, and Church--Rosser transfer |

## Source review outcome

Thirteen source-level defects are corrected with adjacent Bengali notes and exact validator policies.

1. **BN-SRC-292 (OLP-0368):** the frozen grid proof writes two reduction chains but never identifies their endpoints as the arbitrary terms `P` and `Q` used at the end. The target begins from arbitrary `M \xred P` and `M \xred Q` and marks `P_m=P` and `Q_n=Q`, so the completed grid proves the required Church--Rosser conclusion for the transitive closure.
2. **BN-SRC-293 (OLP-0369):** the abstraction rule in the inductive definition assumes ordinary `N \xrightarrow{\beta} N'` even though the conclusion defines parallel beta reduction and both later induction proofs invoke this rule from `N \bredpar N'`. The target restores the parallel premise.
3. **BN-SRC-294 (OLP-0369):** the abstraction case of substitution compatibility drops the prime from the replacement on the right, writing `\Subst{N'}{R}{y}` where the lemma and induction hypothesis require `\Subst{N'}{R'}{y}`. The target restores `R'`.
4. **BN-SRC-295 (OLP-0370):** the frozen proof that one-step beta reduction implies parallel beta reduction assumes every contraction occurs at the root. Since `\bredone` is the smallest compatible relation, the target instead inducts over its derivation, propagates the induction hypothesis through abstraction and both application positions, and retains the source argument for the root-redex case.
5. **BN-SRC-296 (OLP-0370):** the fourth case of the parallel-to-ordinary proof lists `N, M', Q, Q'`, although its premise and every following line require `N, N', Q, Q'`. The target restores the missing reduced-body metavariable `N'`.
6. **BN-SRC-297 (OLP-0371):** the abstraction clause for parallel beta-eta reduction again assumes ordinary beta reduction instead of the relation being defined. The target restores the recursive premise `N \beredpar N'`.
7. **BN-SRC-298 (OLP-0371):** the frozen definition and proofs use raw `FV(N)` at four locations. The target uses the chapter-defined `\FV{N}` notation throughout.
8. **BN-SRC-299 (OLP-0371):** the general abstraction clause and the eta-redex clause of beta-eta complete development overlap, so the frozen clauses do not define a single-valued operation. The target gives the more specific beta and eta redex clauses priority. It also restores the omitted continuation-lemma critical overlap by strong induction. If the eta-redex body uses the application rule, freshness plus the parallel eta rule and the induction hypothesis give the target directly. If that body instead uses the beta rule, a fresh representative of the contracted abstraction is alpha-identical to a smaller abstraction derivation, to which the strong induction hypothesis applies.
9. **BN-SRC-303 (OLP-0371):** the eta case of substitution compatibility quantifies only `x` and `N'`, although its source term, freshness condition, and recursive premise also use the separate body term `N`. The target restores `N` to the witness list.
10. **BN-SRC-304 (OLP-0371):** the same proof substitutes through `\lambd[x][Nx]` and invokes the eta rule without first making the binder fresh for the substitution variable and replacement. The target chooses a fresh alpha-equivalent representative, which prevents capture and preserves the eta freshness condition after substitution and parallel reduction.
11. **BN-SRC-300 (OLP-0372):** the frozen style file declares how `\beredone` is printed, but the mathematical text gives no mixed one-step relation definition for the lemma; its proof then labels a `\bredone` step as eta conversion and cites only reflexivity. The target explicitly reads `\beredone` as one beta or eta contraction, restores `\eredone` in the eta case, carries compatible cases by induction, and uses parallel reflexivity with the parallel eta rule.
12. **BN-SRC-301 (OLP-0372):** the fifth-rule proof case uses raw `FV(N)`. The target normalizes it to `\FV{N}`.
13. **BN-SRC-302 (OLP-0372):** the frozen parallel-to-ordinary proof announces induction but gives only the fifth, eta-rule case. The target states that the first four cases use the four corresponding constructions from the preceding beta-only proof, now inside `\bered`, before treating the fifth rule.

The frozen prose also contains ordinary editorial slips such as “states is,” “height is,” “prove the it,” and the nonstandard plural “redices.” The Bengali text renders the intended prose idiomatically. The editorial warning that recursive functions on lambda terms have not yet been introduced is retained rather than silently resolved.

## Reverse-paraphrase checks

1. **Chapter driver:** the chapter is titled the Church--Rosser property and imports definitions and properties, parallel beta reduction, beta reduction, parallel beta-eta reduction, and beta-eta reduction in exactly the frozen-source order.
2. **Church--Rosser property:** a relation on terms has the property when any two one-step descendants `P` and `Q` of the same term `M` themselves reduce to some common term `N`. The Bengali definition preserves both premises, the existential common reduct, and both outgoing conclusions.
3. **Computation interpretation:** normal-form terms play the role of values and the reduction relation supplies calculation rules. Different available calculation paths therefore retain a single value when the joining property holds; the text does not claim that every term necessarily has a normal form.
4. **Elementary-algebra example:** `4 \times (1+2)+3` can first reduce the parenthesized sum or first use distributivity. The two displayed descendants both continue to `12+3`, and the later final result remains `15`.
5. **Unique normal form:** if `M` reduces to normal forms `P` and `Q`, Church--Rosser supplies a common descendant `N`. Because neither normal form admits a nontrivial further reduction, both paths to `N` are trivial, so `P`, `Q`, and `N` are identical. The Bengali prose preserves the conditional “if any” status of a final result.
6. **Transitive-closure grid:** from arbitrary finite chains ending at `P=P_m` and `Q=Q_n`, the proof builds an `(m+1)` by `(n+1)` grid. Each interior `N_{i,j}` joins its upper and left predecessors under the original relation. The bottom and right boundary chains meet at `N_{m,n}`, which makes `P` and `Q` join under the smallest transitive relation. BN-SRC-292 supplies the endpoint identifications needed for this argument.
7. **Parallel-beta rules:** variables reduce to themselves; abstraction propagates a parallel reduction of its body; application propagates parallel reductions in both components; and a beta redex simultaneously reduces its body and argument before capture-avoiding substitution. BN-SRC-293 keeps all four clauses inside the relation being inductively defined.
8. **Original versus newly created redexes:** one parallel step may contract any number of redexes already present in the original term, but it cannot also contract a redex created by that same step. Thus `(\lambd[f][fx])(\lambd[y][y])` can become itself or `(\lambd[y][y])x`, but needs a second parallel step to reach `x`.
9. **Reflexivity:** every term is parallel-beta-related to itself. The proof is left as an exercise and the following problem asks the reader to supply it; the target preserves both the theorem and that pedagogical division.
10. **Beta-complete development:** `\bcd{M}` recursively preserves variables, develops abstraction bodies, develops both sides of a non-redex application, and develops then substitutes both parts of a beta redex. Unlike an arbitrary parallel reduction, complete development contracts every original redex. The example consequently yields `(\lambd[y][y])x`, not the unchanged source term.
11. **Parallel-beta substitution compatibility:** if both `M` and replacement `R` reduce in parallel, then substituting `R` for `y` in `M` reduces in parallel to the corresponding substitution of `R'` in `M'`. The abstraction case uses the parallel abstraction rule and the induction hypothesis; BN-SRC-294 restores the required primed replacement. The application and redex cases retain the nested substitutions and their variable order.
12. **Parallel-beta continuation:** every one-step parallel reduct `M'` of `M` can reduce in parallel to the complete development `\bcd{M}`. The induction distinguishes a non-abstraction head from an abstraction head in the application case and uses substitution compatibility in the redex case. Any two parallel reducts therefore share `\bcd{M}`, proving Church--Rosser for parallel beta reduction.
13. **Ordinary beta into parallel beta:** the proof proceeds by the derivation of a compatible one-step contraction. The three compatibility constructors use the corresponding parallel constructor and the induction hypothesis; the root contraction uses parallel reflexivity on its body and argument. BN-SRC-295 supplies the cases missing from the frozen proof.
14. **Parallel beta into ordinary beta:** induction over the four parallel rules gives a trivial variable path, lifts the body's ordinary reduction through abstraction, sequences the two component reductions through application, and in the redex case reduces the body and argument before the final root contraction. BN-SRC-296 ensures the reduced body is consistently named `N'`.
15. **Ordinary beta Church--Rosser:** each ordinary one-step beta contraction is a parallel step, while every parallel step expands to an ordinary beta-reduction sequence. Their smallest transitive closures therefore coincide. Church--Rosser transfers from parallel beta reduction to ordinary beta reduction through that equality.
16. **Parallel-beta-eta rules:** the first four clauses are the parallel-beta clauses with the larger relation; the fifth sends `\lambd[x][Nx]` to any parallel reduct `N'` of `N` when `x` is not free in `N`. BN-SRC-297 keeps the abstraction clause recursive in the same relation, and BN-SRC-298 keeps all four freshness tests in the defined free-variable notation.
17. **Beta-eta complete development:** `\becd{M}` has variable, abstraction, application, beta-redex, and eta-redex clauses. The specific beta and eta clauses take priority when they overlap the general application and abstraction clauses, making the operation single-valued. In particular, an eta redex develops to the complete development of its function part.
18. **Parallel-beta-eta substitution compatibility:** the first four induction cases reuse the beta-only constructions. In the eta case, BN-SRC-303 supplies the omitted body witness. BN-SRC-304 first chooses an alpha-equivalent representative whose binder is distinct from the substitution variable and absent from the replacement's free variables. Capture-avoiding substitution through `\lambd[x][Nx]` then yields `\lambd[x][\Subst{N}{R}{y}x]`, retains the eta freshness condition, and the parallel eta rule plus the induction hypothesis reduce it to `\Subst{N'}{R'}{y}`.
19. **Parallel-beta-eta continuation:** the beta-only cases continue to the appropriate complete development. In the abstraction/eta overlap, the body derivation can end either with ordinary parallel application, where the reduct retains the fresh trailing variable and the eta rule applies, or with parallel beta contraction, where alpha-equivalence converts the result to a smaller abstraction derivation. Strong induction handles both branches. The fifth rule reduces `N'` to `\becd{N}`, which equals the complete development of the original eta redex. Thus any two parallel beta-eta reducts share the complete development and the parallel relation has Church--Rosser.
20. **Mixed ordinary step into parallel beta-eta:** `\beredone` is read as either one compatible beta contraction or one compatible eta contraction. Induction carries subterm contractions through the parallel abstraction/application rules; root beta uses the earlier beta proof and root eta uses parallel reflexivity plus the fifth rule. BN-SRC-300 makes every branch explicit and correctly typed.
21. **Parallel beta-eta into ordinary beta-eta:** the first four induction cases reuse the preceding beta constructions inside the larger transitive relation. The fifth first contracts `\lambd[x][Nx]` to `N` by eta and then follows the ordinary beta-eta sequence supplied for `N \beredpar N'`. Consequently the transitive closure of parallel beta-eta reduction is exactly ordinary beta-eta reduction, and Church--Rosser transfers to `\bered`.

## Terminology and authority scope

BN-IN-T290 continues to govern `চার্চ--রসার ধর্ম`, the common-reduct wording, and uniqueness of normal form. BN-IN-T306 continues to govern beta reduction, redexes, normality, and trivial reduction. BN-IN-T308 records `সমান্তরাল $\beta$-হ্রাস` for parallel beta reduction, simultaneous redex contraction, `$\beta$-পূর্ণ বিকাশ` for beta-complete development, complete parallel reduction, and later-created redexes. BN-IN-T309 records `সমান্তরাল $\beta\eta$-হ্রাস`, `$\beta\eta$-পূর্ণ বিকাশ`, the mixed one-step beta-or-eta reading, and priority for the more specific redex clauses.

The checked India-standard pages directly support variables, relations, functions, algebraic expressions, reduction language, and university proof and uniqueness prose. They do not directly attest every Church--Rosser-specific compound. Those choices are definition-governed, explicitly provisional, and open to expert correction. No unlisted dictionary or expert consultation is claimed.

## Validation state

The per-unit checker recognizes all 371 translated target units and passes OLP-0367--OLP-0372 individually. The cumulative index contains 4,275 aligned blocks: 3,627 translated and semantically reviewed, and 648 unchanged structural or formal blocks. This complete-chapter review contributes 72 blocks, 65 translated and seven unchanged. Evidence has 311 source-correction records, three render-equivalent source normalizations, and 309 translation decisions, 290 of them provisional, with 24,410 indexed target-line occurrences and 294 decisions tied to representative exact source wording. The cumulative 299-unit semantic reader remains the current published reader tranche; reader rebuilding is deferred while source translation continues. The 371-unit public source checkpoint has been anonymously verified from its immutable archive and raw translation URLs.
