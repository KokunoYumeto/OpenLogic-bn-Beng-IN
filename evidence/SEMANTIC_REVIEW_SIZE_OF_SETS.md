# Size of Sets — opening draft, 2026-09-04

OLP-0027 chapter driver and OLP-0028 introduction translated after visually consulting NSOU PDF324–325 alongside the previously read mapping/bijection pages. Canon supplies finite/infinite and finite-cardinality terminology. Enumeration/countability and the historical philosophical phrase actual infinity remain provisional; no stronger attestation is claimed.

Reverse paraphrase: distinct a, b and c make {a,b,c} larger than {a,b}; infinite sets need not all have equal size. Some infinite sets permit an infinite list containing every element and others do not. The chapter's editorial driver explicitly retains two approaches: elementary lists/surjections from positive integers and the abstract bijection with the naturals or an initial segment. This convention difference is preserved for later integration, together with all references/imports and Tim Button's attribution. The original words 'a difference definition' are translated as a different definition, correcting grammar without changing the concept.

At this opening checkpoint no later Size of Sets section was translated or claimed; the subsequent dated entries below extend the chapter. These two drafts were outside the 25-unit Functions reader/release candidate.
## OLP-0029 enumeration, 2026-09-04

Complete source compared with the Bengali draft, including all seven list conditions, every exercise, the ceiling-function array, both repetition-removal arguments, both indexing conventions, the empty-set exception and the alternative-definition editorial note. All 32 source/target blocks, control/token signatures and environments match. Formula checking passes after the one exact OLSIZ-001 transformation that restores the omitted table value. The validator additionally protects both arguments of the new usetoken header control.

Reverse paraphrase: every listed member occurs at a finite position in a list with a beginning; repetitions and reordered lists are permitted. An enumeration of nonempty A is a surjection from positive integers; a finite nonempty list extends by repeating its final entry. The empty set is enumerable by a separate clause, without a surjection from the nonempty positive integers. Index shifting uses g(n)=f(n+1) and f(n)=g(n-1). Removing repeated values yields a bijection from positive integers or a finite initial segment. Its construction is an existence argument, not a claim of a computable algorithm. The final characterization by injection into positive integers includes the empty set. All finite/empty cases in exercises remain explicit.

Source notes for later reader integration: the even/odd example declares codomain PosInt while colloquially saying it enumerates its image. Under the formal definition the codomain must be restricted to the even or odd set respectively; the displayed functions are not surjective onto all PosInt. OLSIZ-001 restores the omitted -3 under f(7), whose preceding formula is negative ceiling of 6/2, and discloses that single display correction beside the table. The finite initial segment in the last corollary uses n inclusively, while its proof shifts a positive-length segment to 0 through n-1; the bound variable is existential, so this is a harmless renaming, not a contradictory cardinality claim.
## OLP-0030–0032 zig-zag and pairing, 2026-09-04

Compared all three complete pristine sections with the Bengali drafts after freshly rereading NSOU PDF349 and recovered West Bengal PDF189, alongside previously consulted mapping and inverse pages. Arrays, nested tuples, numerical codes, citations/references and all exercises are retained. The validator caught one omitted occurrence of the formal letter f in the pairing definition; restored it before accepting the draft. OLSIZ-003 is checked as one exact repeated-tuple replacement. New cref references are explicitly protected.

Reverse paraphrase: the diagonal order lists every natural-number pair at a finite index and generalizes to each finite Cartesian power. The Cantor pairing code is (n+m)(n+m+1)/2+n, sending (1,2) to7. Pairing in the given definition requires injectivity, not surjectivity; decoding away from its range can be partial. The alternative code 2^n(2m+1)-1 uses the unique power of two dividing code+1, with examples0,9,51. Encoding by2^n3^m is injective but omits numbers having other prime factors. The cofinite definition is governed by the explicit formula Nat minus A finite; Bengali makes its ambient-set binding explicit rather than reproducing the source's missing word.

Source notes required when these sections enter the reader: (1) the triangle-number paragraph says sum of naturals less than k but gives k(k+1)/2, which instead sums through k; (2) the diagonal-neighbour observation requires m>0; (3) an arbitrary injective pairing function's inverse is only defined on its range, so it need not itself be an enumeration with domain Nat/PosInt; enumerate its range first, and handle an empty product separately; (4) countably many enumerable sets require a suitable choice principle to choose their enumerations in the unrestricted statement; this is an assumption to state, not a request for human approval; (5) pairing-alt calls the second pair (0,2) while its table correctly has (0,1). OLSIZ-002 adds keyed provenance for the already-correct Bengali cofinite definition. OLSIZ-003 replaces the duplicated second (2,m) family with (3,m), as fixed by the immediately following table. The nested-pair identification of triples and the zero-fold product convention follow the earlier source's conventions, rather than asserting literal set equality across incompatible tuple encodings.
## OLP-0033–0035 diagonalization, reduction and equinumerosity, 2026-09-04

All 56 source/target blocks across these three sections were compared, including both diagonal arrays, the two characteristic-sequence constructions, every reduction-direction counterexample, all exercises, the Frege quotation/citation and both conditional enumeration proof branches. Formula, environment, protected-control, token and NFC checks pass. The formula deltas are the exact OLSIZ-004 bound-name normalization, the OLSIZ-005 infinite-tail repair and BN-SRC-001 / OLSIZ-006, an exact g-to-f correction in a proof whose governing bijection is f; adjacent target notes and SOURCE_CORRECTIONS.jsonl preserve every frozen-source location and hash.

Reverse paraphrase: any proposed list of infinite binary sequences yields a binary sequence differing from row k at position k. Any proposed list of positive-integer subsets yields the subset containing n exactly when its nth listed set omits n. Reduction proves nonenumerability of A by turning any enumeration of A into one of a known nonenumerable B, using a surjection A to B or equivalently an injection B to A. The characteristic-sequence map from the power set of positive integers onto binary sequences has the required direction. Equinumerosity means existence of a bijection and is reflexive by identity, symmetric by inverse and transitive by composition; it transfers enumerability in both directions, including the empty-set case.

Source presentation corrections: OLSIZ-004 replaces the unbound output label s_k by the single bound name s throughout the primary characteristic-sequence definition. OLSIZ-005 extends the displayed finite run of n zeros by an infinite tail of ones, so every h(n) belongs to the declared codomain of infinite binary sequences while the all-zero sequence witnesses non-surjectivity. BN-SRC-001 / OLSIZ-006 corrects the undefined g in OLP-0035's empty-A subargument to f. In the alternative conditional branch, g is properly introduced as an enumeration. No public reader or release claim is made for these Size of Sets drafts.
## OLP-0036–0037 cardinal comparison and Schröder–Bernstein, 2026-09-04

Compared both complete source sections, both conditional Cantor proof variants, the final injection exercise, citation, historical footnote and deferred-proof reference. All 22 source/target blocks and formal signatures pass. BN-SRC-002 corrects one quantified-domain expression from x in A-bar to x in A; the exact validator exception and adjacent note bind it to the frozen source hash.

Reverse paraphrase: A is no larger than B when an injection A to B exists; it is strictly smaller when that injection exists but no bijection does. The singleton map injects A into its power set. Cantor's diagonal subset contains exactly those x omitted by g(x), so it differs from each g(x), ruling out a surjection and hence a bijection. The faster branch takes the same contradiction from a putative bijection. Schröder–Bernstein says injections in both directions yield a bijection; this section postpones its difficult proof but accurately explains its use.

Foundational qualification for the eventual reader note: the displayed equivalence between nonenumerability and Nat being strictly smaller assumes the surrounding classical setting where every infinite set has a countably infinite subset; that implication is not valid for Dedekind-finite infinite sets in ZF without relevant choice. The source later discusses cardinal foundations; the aligned text is preserved, and this qualification does not create a review hold. Proper-name spelling remains in original controls/citations while the Bengali heading transliterates it.

## OLP-0038–0040 alternate enumeration, diagonalization and reduction, 2026-09-04

Compared all three complete alternate sections with the Bengali drafts, including active and commented material. All 51 source/target blocks, protected controls, token signatures, environments and NFC checks pass; formula checking includes the exact BN-SRC-005 / OLSIZ-010 output-name normalization. The TeX comment status of the optional reduction explanation and partial-function exercise remains unchanged, so the translation does not silently activate upstream-disabled material.

Reverse paraphrase: the set-theoretic alternative defines an enumeration as a bijection from either all naturals or a finite initial segment and separately counts the empty set as enumerable. A binary diagonal sequence differs from row n at entry n, and the analogous subset D contains n exactly when the nth listed subset omits n. Reduction turns an enumeration of the power set of the naturals into one of the binary sequences using the membership-indicator map. The injection exercise states the equivalent reverse criterion, and every exercise retains its original direction and domain.

BN-SRC-003 / OLSIZ-008 corrects the prose description of $s_n(m)$: the table and notation make it the mth digit of the nth string, while the frozen English sentence reverses those roles. BN-SRC-004 / OLSIZ-009 corrects the duplicated instruction to change every 1 to 0; the displayed cases require changing 0 to 1 and 1 to 0. BN-SRC-005 / OLSIZ-010 now normalizes the unbound $s_k$ expression itself to the single output name $s$, rather than merely explaining the intended membership-indicator sequence. Each note is adjacent to the affected Bengali passage and is carried into the hash-bound source-correction ledger. The later use of $N_1,N_2,\ldots$ is an informal one-based display of a list even though the abstract enumeration has domain $\Nat$; it does not remove the zeroth domain value or change the surjectivity argument.

The three alternate files complete the source chapter's editorial choice alongside the longer primary path. A reader integration must label the two conventions clearly and preserve their cross-references; it must not concatenate them as though they were consecutive definitions in a single unqualified exposition.

## Shared rotating source audit reconciliation, 2026-09-04

Reconciled the complete shared OLSIZ-20260904 audit against the pristine source revision and the Bengali files. The machine authority is `FINDINGS.json`, 12,592 bytes, SHA-256 `9b6e836c8432eb75d331913983603796d6557da6ec3ad71b846b2a248374cd07`; its readable review has SHA-256 `26913176baacbda5ae8a47bcc82ccf0366b0763313e6eeeb45df59e64249a1f9`. The audit independently replayed all 537 segments in the prior 39-unit checkpoint, resolved every used canon passage and found no hash, range or locator failure.

All ten deterministic OLSIZ findings are now repaired or explicitly clarified in the Bengali source. OLSIZ-001 through OLSIZ-005 receive new shared-ID notes and correction records. The pre-existing BN-SRC-001 through BN-SRC-005 records are cross-linked to OLSIZ-006 through OLSIZ-010, and their adjacent notes carry both identifiers. BN-SRC-005 / OLSIZ-010 is upgraded from a preserved-formula clarification to an actual bound-name correction. The validator constructs the exact audited source-formula expectation for OLSIZ-001, OLSIZ-003, OLSIZ-004, OLSIZ-005 and OLSIZ-010; all remaining formula, block, control, environment, token and NFC checks stay in force.

The manager later retracted the OLSIZ-011 alert as a false positive after independent byte-level checks exposed an overlapping-pattern counting error. At all five cited source offsets, the byte sequence is `5c5c5c686c696e65`: three backslashes followed by `hline`. TeX reads this as the `\\` row-break control followed by the `\hline` control word. The frozen source and Bengali target each contain five such tokens and zero isolated two-backslash-plus-literal-`hline` tokens. No correction or adjacent correction note is appropriate. The corrected supplement, public disposition record and validator preserve the counts, offsets, hashes, compiler smoke test and retraction.
